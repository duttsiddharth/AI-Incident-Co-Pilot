[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-incident-copilot"
version = "1.0.0"
description = "AI-powered IT incident ticket analysis with RAG"
requires-python = ">=3.11"
